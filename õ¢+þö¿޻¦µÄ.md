# Token Monitor v1 使用说明

AI API Token 用量监控桌面工具。通过本地代理实时追踪 DeepSeek / OpenAI / Anthropic 等 API 的 Token 消耗和消费金额，支持余额查询和预警。

---

## 快速开始

### 已编译版本（开箱即用）

直接运行 `token-monitor.exe` 即可启动，无需安装任何依赖。

程序启动后会在系统托盘显示图标，同时自动启动本地代理服务（端口 `5099`）。

### 从源码运行

```bash
# Python 依赖
pip install flask flask-cors

# 启动后端
python backend/server.py

# 启动前端（另开终端）
npm install
npm start
```

---

## 配置 API 密钥

点击窗口右上角 **齿轮图标** 打开设置面板：

| 字段 | 说明 |
|---|---|
| 密钥 | API Key（如 DeepSeek 的 `sk-...`） |
| API 地址 | API Base URL，如 `https://api.deepseek.com` |
| 充值链接 | 点击"充值"按钮时打开的网页 |
| 余额预警 | 余额低于此值（美元）时显示黄色警告 |
| 月度预算 | 当月消费上限，超出后高亮提醒 |

支持配置 **最多 3 组** API 密钥，每组独立设置 API 地址、充值链接和预警线。

### 自动记录消耗

将 LLM 客户端（Cursor、Claude Code、ChatGPT 桌面端等）的 API Base 设置为：

```
http://127.0.0.1:5099/v1
```

所有请求经过本地代理后自动记录 Token 消耗和费用，无需手动操作。

支持的代理协议：
- **OpenAI 兼容** — `/v1/chat/completions`（所有兼容 OpenAI 格式的 API）
- **Anthropic Messages** — `/v1/messages`（Claude Code 等使用 Anthropic 协议的客户端，含 SSE 流式）

---

## 功能说明

### 仪表盘
- **实时余额**：自动查询 DeepSeek 账户余额（充值金额 + 赠送金额）
- **今日/本周/本月消费**：基于余额快照差值精确计算，非估算值
- **月度预算**：设置预算后环形图展示已用/剩余比例
- **请求量**：今日请求次数

### 用量图表
- 柱状图展示每日 Token 消耗趋势
- 支持**周 / 月 / 年**三种视图切换

### 模型明细
- 按模型拆分 Token 用量占比（饼图）
- 每个模型的请求次数和消费金额

### 消费分析
- 各模型独立费用统计
- 今日 / 本周 / 本月消费分层展示

### 窗口固定
点击 **图钉按钮**：
- 窗口置顶且无法最小化
- 按 Win+D 显示桌面后自动恢复
- 再次点击取消固定

### 开机自启
设置面板中勾选 **"开机自动启动"**，系统启动时自动运行。

---

## 常见问题

**Q: 余额显示 "--"？**
A: 确认 API Key 已正确填写，且网络能访问 `api.deepseek.com`。仅 DeepSeek API 支持余额自动查询。

**Q: 代理不生效？**
A: 确认 `token-monitor.exe` 正常运行（系统托盘有图标），检查客户端的 API Base 是否设为 `http://127.0.0.1:5099/v1`。

**Q: 如何卸载开机自启？**
A: 在设置面板取消勾选"开机自动启动"，或手动删除 `shell:startup` 文件夹中的快捷方式。

**Q: 配置文件保存在哪里？**
A: `config.json` 和数据库 `data/usage.db` 均保存在程序所在目录下。重装或移动程序时备份这两个文件即可保留历史数据。

**Q: 支持哪些模型定价？**
A: 内置 DeepSeek 系列定价（v4-pro、v4-flash、chat、reasoner），其他模型按 `deepseek-chat` 默认价格估算。

---

## 文件结构

```
token-monitor-v1/
├── token-monitor.exe    # 桌面客户端（Electron）
├── icon.png / icon64.png # 图标
├── backend/
│   ├── backend.exe      # Flask 后端（代理 + 数据存储）
│   └── server.py        # 后端源码
├── config.example.json  # 配置模板
├── config.json          # 用户配置（自动生成，不要提交到 Git）
└── data/                # 数据库存放目录（自动生成）
    └── usage.db
```
